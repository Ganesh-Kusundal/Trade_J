# Optional API base for split hosting (e.g. https://api.example.com). Leave empty for same-origin.
# VITE_API_BASE=

# Vite dev-server proxy target (default http://localhost:8080)
# VITE_API_PROXY_TARGET=http://localhost:8080
