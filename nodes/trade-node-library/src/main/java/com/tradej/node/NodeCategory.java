package com.tradej.node;

/** Broad category for a pipeline node type. */
public enum NodeCategory {
    DATA_INGEST,
    FEATURE,
    SCANNER,
    FILTER,
    RANK,
    UNIVERSE,
    SIGNAL,
    RISK,
    EXECUTION,
    OUTPUT,
    UTILITY
}
