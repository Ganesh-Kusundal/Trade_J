package com.tradej.node;

/**
 * PHASE 2 of the Trade-J platform transformation.
 *
 * <p>Self-describing node library. Every node type in the platform must
 * provide a {@link NodeDescriptor} so the frontend can render configuration
 * forms without hardcoding.
 */
class PackageInfo {
    private PackageInfo() {}
}
